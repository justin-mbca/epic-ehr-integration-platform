package com.epicehr.fhir.config;

import org.springframework.context.annotation.Configuration;

/**
 * Servlet configuration placeholder for the FHIR server.
 *
 * The original file mistakenly contained a duplicate controller class
 * (moved to `com.epicehr.fhir.controller.FhirController`). Keep this
 * class minimal to avoid accidental duplicate type declarations.
 */
@Configuration
public class FhirServletConfig {
    // Reserved for servlet-specific configuration (servlet registration,
    // filters, or additional DispatcherServlet settings) if needed later.
}
