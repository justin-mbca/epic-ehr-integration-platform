services:
  # Development Database
  postgres-dev:
    image: postgres:15-alpine
    container_name: epic-postgres-dev
    environment:
      POSTGRES_DB: epic_ehr_dev
      POSTGRES_USER: dev_user
      POSTGRES_PASSWORD: dev_password
    ports:
      # Remapped host port to avoid conflict with local Postgres
      - "15432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    networks:
      - epic-dev-network

  # Development Redis
  redis-dev:
    image: redis:7-alpine
    container_name: epic-redis-dev
    ports:
      # Remapped host port to avoid conflict with local Redis
      - "16379:6379"
    networks:
      - epic-dev-network

  # PgAdmin for Database Management
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: epic-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@epic.local
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      # Remapped host port to avoid conflict with host services
      - "15050:80"
    depends_on:
      - postgres-dev
    networks:
      - epic-dev-network

networks:
  epic-dev-network:
    driver: bridge

volumes:
  postgres_dev_data:
